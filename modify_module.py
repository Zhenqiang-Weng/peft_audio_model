# trainer.py 3110
# import pandas as pd
#
# predictions = torch.nn.functional.softmax(torch.from_numpy(output.predictions), dim=-1).numpy()
# dataFrame = pd.DataFrame(predictions, index=eval_dataset['uid'])
# dataFrame.to_csv(os.path.join(self.args.output_dir, 'detailInformation.csv'), sep=',')

# trainer_pt_utils.py 508
# 添加ratio
